package com.rzrasel.rztutorial.utils

object AppConstants {
    object Assets {
        object Json {
            private const val jsonDirectory: String = "json"
            const val testJson: String = "${jsonDirectory}/test.json"
            const val jsonDashboardMenuBn = "${jsonDirectory}/bn_dashboard_menu.json"
            const val dashboardMenuEn = "${jsonDirectory}/En-Dashboard-Menu.json"
            const val humanBodyPartsBn = "${jsonDirectory}/Bn-Human-Body-Parts.json"
            const val humanBodyPartsEn = "${jsonDirectory}/En-Human-Body-Parts.json"
            const val jsonLessonDataModel = "${jsonDirectory}/lesson_data_model.json"
        }
    }
}